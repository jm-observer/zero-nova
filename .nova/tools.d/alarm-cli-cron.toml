# Auto-generated tool configuration for: alarm-cli cron
# Generated by mcp-tool-generator

[[tools]]
name = "alarm_cli_cron"
description = "Create a recurring cron alarm that fires on a schedule and repeats indefinitely using 5-field or 6-field cron syntax."
type = "command"
command = "alarm-cli"
subcommands = ["cron"]
cwd = false

[[tools.parameters]]
name = "name"
description = "Human-readable alarm name for identification in logs and dashboard"
type = "string"
required = false
arg = ["--name"]

[[tools.parameters]]
name = "cron"
description = 'Cron expression: "sec min hour day-of-month day-of-week" (6-field) or "min hour dom dow" (5-field). Month field is not supported.'
type = "string"
required = true
arg = ["--cron"]

[[tools.parameters]]
name = "callback_url"
description = "HTTP URL to POST when the alarm fires. Server sends callback_body as JSON payload"
type = "string"
required = true
arg = ["--callback-url"]

[[tools.parameters]]
name = "callback_body"
description = """Optional JSON object sent as POST body to callback_url, e.g. '{"msg":"hello"}'"""
type = "string"
required = false
arg = ["--callback-body"]

[[tools.parameters]]
name = "workspace"
description = "Workspace directory where config.toml and database are stored"
type = "string"
required = false
arg = [
    "--workspace",
    "-w",
]

[[tools.parameters]]
name = "server"
description = "Server base URL. If not set, reads port from workspace config.toml and uses http://127.0.0.1:{port}"
type = "string"
required = false
arg = ["--server"]
