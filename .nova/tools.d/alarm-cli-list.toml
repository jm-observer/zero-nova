# Auto-generated tool configuration for: alarm-cli list
# Generated by mcp-tool-generator

[[tools]]
name = "alarm_cli_list"
description = "List all alarms, optionally filtered by status. Returns JSON array with alarm details including next fire time"
type = "command"
command = "alarm-cli"
subcommands = ["list"]
cwd = false

[[tools.parameters]]
name = "status"
description = 'Filter by alarm status: "active" (pending/recurring) or "completed" (finished one-time alarms)'
type = "string"
required = false
arg = ["--status"]

[[tools.parameters]]
name = "workspace"
description = "Workspace directory where config.toml and database are stored (default: ~/.config/alarm-server)"
type = "string"
required = false
arg = [
    "--workspace",
    "-w",
]

[[tools.parameters]]
name = "server"
description = "Server base URL. If not set, reads port from workspace config.toml and uses http://127.0.0.1:{port} [default: http://127.0.0.1:8080]"
type = "string"
required = false
arg = ["--server"]
