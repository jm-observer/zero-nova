# Auto-generated tool configuration for: alarm-cli once
# Generated by mcp-tool-generator

[[tools]]
name = "alarm_cli_once"
description = "Create a one-time alarm that fires once at a specific datetime then marks itself completed."
type = "command"
command = "alarm-cli"
subcommands = ["once"]
cwd = false

[[tools.parameters]]
name = "name"
description = "Human-readable alarm name for identification in logs and dashboard"
type = "string"
required = false
arg = ["--name"]

[[tools.parameters]]
name = "once_at"
description = "Trigger datetime in format YYYY-MM-DDTHH:MM:SS (local time, must be in the future)"
type = "string"
required = true
arg = ["--once-at"]

[[tools.parameters]]
name = "callback_url"
description = "HTTP URL to POST when the alarm fires. Server sends callback_body as JSON payload"
type = "string"
required = true
arg = ["--callback-url"]

[[tools.parameters]]
name = "callback_body"
description = """Optional JSON object sent as POST body to callback_url, e.g. '{"msg":"hello"}'"""
type = "string"
required = false
arg = ["--callback-body"]

[[tools.parameters]]
name = "workspace"
description = "Workspace directory where config.toml and database are stored (default: ~/.config/alarm-server)"
type = "string"
required = false
arg = [
    "--workspace",
    "-w",
]

[[tools.parameters]]
name = "server"
description = "Server base URL. If not set, reads port from workspace config.toml and uses http://127.0.0.1:{port}"
type = "string"
required = false
arg = ["--server"]
